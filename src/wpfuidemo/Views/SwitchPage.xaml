<UserControl x:Class="wpfuidemo.Views.SwitchPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:Plant01.WpfUI.Controls;assembly=Plant01.WpfUI">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="AntSwitch" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.ColumnSpan="2"/>

        <!-- Preview Area -->
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="#E8E8E8" BorderThickness="1" CornerRadius="4" Margin="0,0,20,0" Background="White">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:AntSwitch x:Name="DemoSwitch" 
                              CheckedContent="Open" 
                              UnCheckedContent="Close"/>
            </Grid>
        </Border>

        <!-- Settings Panel -->
        <Border Grid.Row="1" Grid.Column="1" BorderBrush="#E8E8E8" BorderThickness="1" Padding="15" CornerRadius="4" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Configuration" FontWeight="Bold" Margin="0,0,0,15" FontSize="16"/>

                    <!-- Text Placement -->
                    <TextBlock Text="Text Placement" Margin="0,0,0,5" Foreground="#666"/>
                    <ComboBox x:Name="PlacementCombo" SelectedIndex="0" Margin="0,0,0,15" SelectionChanged="Placement_SelectionChanged">
                        <ComboBoxItem Content="Inside"/>
                        <ComboBoxItem Content="Left"/>
                        <ComboBoxItem Content="Right"/>
                    </ComboBox>

                    <!-- Checked Content -->
                    <TextBlock Text="Checked Content" Margin="0,0,0,5" Foreground="#666"/>
                    <TextBox Text="{Binding ElementName=DemoSwitch, Path=CheckedContent, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Padding="4"/>

                    <!-- Unchecked Content -->
                    <TextBlock Text="Unchecked Content" Margin="0,0,0,5" Foreground="#666"/>
                    <TextBox Text="{Binding ElementName=DemoSwitch, Path=UnCheckedContent, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Padding="4"/>

                    <!-- Toggles -->
                    <CheckBox Content="Loading" IsChecked="{Binding ElementName=DemoSwitch, Path=Loading}" Margin="0,0,0,10"/>
                    <CheckBox Content="Disabled" Margin="0,0,0,10" Checked="DisabledCheck_Changed" Unchecked="DisabledCheck_Changed"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
