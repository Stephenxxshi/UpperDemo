<UserControl x:Class="wpfuidemo.Views.DialogPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Plant01.WpfUI.Controls;assembly=Plant01.WpfUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="AntModal" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <TextBlock Text="Basic Modal" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <ui:AntButton Content="Open Modal (Blur)" Click="OpenModal_Click" Type="Primary" Margin="0,0,10,0"/>
                <ui:AntButton Content="Open Modal (Dim)" Click="OpenDimModal_Click" Margin="0,0,10,0"/>
                <ui:AntButton Content="Open Modal (None)" Click="OpenNoneModal_Click"/>
            </StackPanel>
            
            <TextBlock Text="Custom Footer" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <ui:AntButton Content="Open Custom Modal" Click="OpenCustomModal_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
