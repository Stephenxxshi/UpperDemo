<UserControl x:Class="wpfuidemo.Views.TimelinePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="clr-namespace:Plant01.WpfUI.Controls;assembly=Plant01.WpfUI"
      xmlns:themes="clr-namespace:Plant01.WpfUI.Themes;assembly=Plant01.WpfUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="AntTimeline" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.ColumnSpan="2"/>

        <!-- Preview Area -->
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="{DynamicResource {x:Static themes:DesignTokenKeys.BorderColor}}" BorderThickness="1" CornerRadius="4" Margin="0,0,20,0" Background="{DynamicResource {x:Static themes:DesignTokenKeys.ComponentBackground}}">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <controls:AntTimeline x:Name="DemoTimeline">
                    <controls:AntTimelineItem Color="Green">
                        <TextBlock Text="Create a services site 2015-09-01"/>
                    </controls:AntTimelineItem>
                    <controls:AntTimelineItem Color="Blue">
                        <TextBlock Text="Solve initial network problems 2015-09-01"/>
                    </controls:AntTimelineItem>
                    <controls:AntTimelineItem Color="Red">
                        <TextBlock Text="Technical testing 2015-09-01"/>
                    </controls:AntTimelineItem>
                    <controls:AntTimelineItem Color="Gray">
                        <TextBlock Text="Network problems being solved 2015-09-01"/>
                    </controls:AntTimelineItem>
                </controls:AntTimeline>
            </Grid>
        </Border>

        <!-- Settings Panel -->
        <Border Grid.Row="1" Grid.Column="1" BorderBrush="{DynamicResource {x:Static themes:DesignTokenKeys.BorderColor}}" BorderThickness="1" Padding="15" CornerRadius="4" Background="{DynamicResource {x:Static themes:DesignTokenKeys.ComponentBackground}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Configuration" FontWeight="Bold" Margin="0,0,0,15" FontSize="16"/>

                    <!-- Mode -->
                    <TextBlock Text="Mode" Margin="0,0,0,5" Foreground="{DynamicResource {x:Static themes:DesignTokenKeys.TextSecondary}}"/>
                    <ComboBox x:Name="ModeCombo" SelectedIndex="0" Margin="0,0,0,15" SelectionChanged="ModeCombo_SelectionChanged">
                        <ComboBoxItem Content="Left"/>
                        <ComboBoxItem Content="Right"/>
                        <ComboBoxItem Content="Alternate"/>
                    </ComboBox>

                    <!-- Toggles -->
                    <CheckBox Content="Reverse" IsChecked="{Binding ElementName=DemoTimeline, Path=Reverse}" Margin="0,0,0,10"/>
                    <CheckBox Content="Pending" IsChecked="{Binding ElementName=DemoTimeline, Path=Pending}" Margin="0,0,0,10"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>


