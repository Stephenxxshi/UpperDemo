<Window x:Class="wpfuidemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Plant01.WpfUI.Controls;assembly=Plant01.WpfUI"
        xmlns:themes="clr-namespace:Plant01.WpfUI.Themes;assembly=Plant01.WpfUI"
        mc:Ignorable="d"
        Title="WpfUI Demo" Height="600" Width="900"
        Background="{DynamicResource {x:Static themes:DesignTokenKeys.ComponentBackground}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Menu -->
        <Border BorderBrush="{DynamicResource {x:Static themes:DesignTokenKeys.BorderColor}}" 
                BorderThickness="0,0,1,0"
                Background="{DynamicResource {x:Static themes:DesignTokenKeys.ComponentBackground}}">
            <controls:AntMenu x:Name="NavMenu" Margin="0,10,0,0" SelectedItemChanged="NavMenu_SelectedItemChanged">
                <controls:AntMenuItem Header="General" Icon="🏠" IsExpanded="True">
                    <controls:AntMenuItem Header="Dashboard" Tag="Dashboard" IsSelected="True"/>
                    <controls:AntMenuItem Header="Button 按钮" Tag="Button"/>
                </controls:AntMenuItem>
                <controls:AntMenuItem Header="Layout" Icon="📐" IsExpanded="True">
                    <controls:AntMenuItem Header="Space 间距" Tag="Layout"/>
                    <controls:AntMenuItem Header="Divider 分割线" Tag="Layout"/>
                    <controls:AntMenuItem Header="Splitter 分割面板" Tag="Layout"/>
                </controls:AntMenuItem>
                <controls:AntMenuItem Header="Data Entry" Icon="📝" IsExpanded="True">
                    <controls:AntMenuItem Header="Input 输入框" Tag="Input"/>
                    <controls:AntMenuItem Header="Checkbox 多选框" Tag="Checkbox"/>
                    <controls:AntMenuItem Header="DatePicker 日期选择" Tag="DatePicker"/>
                    <controls:AntMenuItem Header="Calendar 日历" Tag="Calendar"/>
                    <controls:AntMenuItem Header="Switch 开关" Tag="Switch"/>
                </controls:AntMenuItem>
            </controls:AntMenu>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
                <!-- Color Palette -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0" VerticalAlignment="Center">
                    <TextBlock Text="Theme: " VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource {x:Static themes:DesignTokenKeys.TextPrimary}}"/>
                    <Button Width="20" Height="20" Margin="2" Background="#1677ff" Click="Color_Click" Tag="#1677ff" ToolTip="Daybreak Blue" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="20" Height="20" Margin="2" Background="#f5222d" Click="Color_Click" Tag="#f5222d" ToolTip="Dust Red" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="20" Height="20" Margin="2" Background="#fa8c16" Click="Color_Click" Tag="#fa8c16" ToolTip="Sunset Orange" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="20" Height="20" Margin="2" Background="#faad14" Click="Color_Click" Tag="#faad14" ToolTip="Calendula Gold" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="20" Height="20" Margin="2" Background="#13c2c2" Click="Color_Click" Tag="#13c2c2" ToolTip="Cyan" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="20" Height="20" Margin="2" Background="#52c41a" Click="Color_Click" Tag="#52c41a" ToolTip="Polar Green" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                    <Button Width="20" Height="20" Margin="2" Background="#722ed1" Click="Color_Click" Tag="#722ed1" ToolTip="Golden Purple" Style="{DynamicResource AntDesign.Button.Base}" Cursor="Hand" BorderThickness="0"/>
                </StackPanel>

                <Button Content="Toggle Dark Mode" Click="ToggleTheme_Click" Style="{DynamicResource AntDesign.Button.Default}" Margin="0,0,10,0"/>
                
                <ComboBox x:Name="DensityCombo" SelectedIndex="1" SelectionChanged="Density_SelectionChanged" Width="100" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="Compact" Tag="Compact"/>
                    <ComboBoxItem Content="Default" Tag="Default"/>
                    <ComboBoxItem Content="Touch" Tag="Touch"/>
                </ComboBox>
            </StackPanel>

            <!-- Page Content -->
            <ContentControl x:Name="MainContent" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
