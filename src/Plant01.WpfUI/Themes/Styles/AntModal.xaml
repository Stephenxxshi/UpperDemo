<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:themes="clr-namespace:Plant01.WpfUI.Themes"
                    xmlns:controls="clr-namespace:Plant01.WpfUI.Controls"
                    xmlns:converters="clr-namespace:Plant01.WpfUI.Converters">

    <converters:ThicknessSideConverter x:Key="ThicknessSideConverter"/>

    <Style TargetType="{x:Type controls:AntModal}">
        <Setter Property="Background" Value="{DynamicResource {x:Static themes:DesignTokenKeys.ComponentBackground}}"/>
        <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:DesignTokenKeys.TextPrimary}}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:DesignTokenKeys.BorderColorSplit}}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="{DynamicResource {x:Static themes:DesignTokenKeys.PaddingLG}}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="{DynamicResource {x:Static themes:DesignTokenKeys.FontSize}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:AntModal}">
                    <Border Background="Transparent">
                        <!-- Shadow Container -->
                        <Border Background="{DynamicResource {x:Static themes:DesignTokenKeys.ComponentBackground}}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource {x:Static themes:DesignTokenKeys.BorderRadius}}"
                                Margin="20"
                                MinWidth="400">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2" Direction="270"/>
                            </Border.Effect>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/> <!-- Header -->
                                    <RowDefinition Height="*"/>    <!-- Content -->
                                    <RowDefinition Height="Auto"/> <!-- Footer -->
                                </Grid.RowDefinitions>

                                <!-- Header -->
                                <Grid Grid.Row="0" Margin="{DynamicResource {x:Static themes:DesignTokenKeys.ModalHeaderPadding}}">
                                    <TextBlock Text="{TemplateBinding Title}" 
                                               FontWeight="SemiBold" 
                                               FontSize="{DynamicResource {x:Static themes:DesignTokenKeys.FontSizeLG}}"/>
                                    <Button Command="{x:Static SystemCommands.CloseWindowCommand}"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Width="22" Height="22"
                                            Margin="0,-4,-8,0"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Padding="0">
                                        <Path Data="M 0 0 L 10 10 M 0 10 L 10 0" 
                                              Stroke="{DynamicResource {x:Static themes:DesignTokenKeys.TextQuaternary}}" 
                                              StrokeThickness="1.5"
                                              Stretch="Uniform"
                                              Width="10" Height="10"/>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Cursor" Value="Hand"/>
                                                                    <Setter Property="Background" Value="{DynamicResource {x:Static themes:DesignTokenKeys.ControlItemBgHover}}"/>
                                                                    <Setter Property="Opacity" Value="1"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>

                                <!-- Content -->
                                <ContentPresenter Grid.Row="1" Margin="{DynamicResource {x:Static themes:DesignTokenKeys.ModalContentPadding}}"/>

                                <!-- Footer -->
                                <Border Grid.Row="2" 
                                        BorderBrush="{DynamicResource {x:Static themes:DesignTokenKeys.BorderColorSplit}}"
                                        BorderThickness="0,0,0,0"
                                        Padding="{DynamicResource {x:Static themes:DesignTokenKeys.ModalFooterPadding}}"
                                        Background="Transparent"
                                        CornerRadius="0,0,6,6">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <controls:AntButton Content="{TemplateBinding CancelText}" 
                                                            Type="Default" 
                                                            Margin="0,0,8,0"
                                                            Command="{Binding CancelCommand, RelativeSource={RelativeSource TemplatedParent}}"/>
                                        <controls:AntButton Content="{TemplateBinding OkText}" 
                                                            Type="Primary"
                                                            Command="{Binding OkCommand, RelativeSource={RelativeSource TemplatedParent}}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
